import 'package:flutter/material.dart';

class AppGradients {
  static const Gradient linearRoxoHomepage = LinearGradient(
    colors: [Color(0xFF8B1FF8), Color(0xFF041832), Color(0xFF01080E)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );

  static const Gradient linearVerde = LinearGradient(
    colors: [Color(0xFF0F725C), Color(0xFF041832), Color(0xFF01080E)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );

  static const Gradient linearAzul = LinearGradient(
    colors: [Color(0xFF1875E9), Color(0xFF041832), Color(0xFF01080E)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );

  static const Gradient linearBotoes = LinearGradient(
    colors: [Color(0xFFB872FF), Color(0xFF144480)],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
}
